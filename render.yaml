services:
  - type: web
    name: wit-awx-leaderboard
    runtime: python
    buildCommand: pip install -r server/requirements.txt
    startCommand: gunicorn server.main:app --bind 0.0.0.0:$PORT
    envVars:
      - key: ADMIN_KEY
        generateValue: true
